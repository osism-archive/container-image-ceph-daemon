---
dist: bionic
language: generic

before_install:
  - scripts/travis.sh

env:
  global:
    - REPOSITORY=osism/ceph-daemon
  matrix:
    - VERSION=latest
    - VERSION=v3.2.7-stable-3.2-luminous
    - VERSION=v3.2.8-stable-3.2-luminous
    - VERSION=v3.2.10-stable-3.2-luminous
    - VERSION=v4.0.9-stable-4.0-nautilus
    - VERSION=v4.0.10-stable-4.0-nautilus

before_script:
  - bash scripts/lint.sh

script:
  - bash scripts/build.sh

notifications:
  irc:
    channels:
      - "irc.freenode.org#osism"
    template:
      - "%{result} %{repository_slug}: %{commit_subject} %{build_url}"
    skip_join: true
